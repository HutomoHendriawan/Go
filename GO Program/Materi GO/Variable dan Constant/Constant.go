package main

import main (
		"fmt"
)

func main() {
		const judul = "Ini Judul"
		fmt.Println(judul)
		// jika kode di bawah ini di-uncomment maka akan error
		// judul = "judul diubah"
}

package main

import (
		"fmt"
)

func main () {
		const judul = "Ini apaan ya"
		fmt.Println(judul)
		// jika kode di bawah ini di-uncomment maka akan error
		// judul = "Ini apaan ya diubah"
}

package main

import (
		"fmt"
)

func main() {
		const judul = "Hai Endik"
		fmt.Println(judul)
		// jika kode di bawah ini di-uncomment maka akan error
		// judul = "Ini apaan ya diubah"
}

package main

import (
		"fmt"
)

func main () {
		const judul = "Mulai lancar"
		fmt.Println(judul)
		// jika kode di bawah ini di-uncomment maka akan error
		// judul = "Ini apaan ya diubah"
}

package main

import main (
		"fmt"
)

func main() {
		const judul = "Next step kuy"
		fmt.Println(judul)
		// jika kode di bawah ini di-uncomment maka akan error
		// judul = "Ini apaan ya diubah"
}